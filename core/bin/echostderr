#!/bin/env sh

function echostderr__() {
  ([ -e /proc/self/fd/2 ] && (echo "${@}" >>/proc/self/fd/2)) \
    || ([ -e /dev/stderr ] && (echo "${@}" >>/dev/stderr)) \
    || return 10
}

echostderr__ "${@}"
