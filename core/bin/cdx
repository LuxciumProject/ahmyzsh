#!/usr/bin/env sh
# builtin cd $
# # mypath="${@:-$HOME}"
# #   echo ''
# #   builtin cd "$(echo $mypath)"
# function cdxy() {
#   catchupPATH=$(/bin/pwd)
#   tentativePath=${@:-${HOME}}
#   effectivePATH="$(builtin cd ${tentativePath} 2>/dev/null && echo "${tentativePath}" || echo ${catchupPATH})"
#   echo "${effectivePATH}"
# }
# # builtin cd

# builtin cd /tmp #$(echo $(cdxy /tmp))
# pwd
# function cdx_() {
#   catchupPATH=$(/bin/pwd)
#   tentativePath=${@:-${HOME}}
#   # echo $currentPWD
#   #
#   effectivePATH="$(builtin cd ${tentativePath} 2>/dev/null && echo "${tentativePath}" || echo $catchupPATH)"
#   builtin cd "${effectivePATH}"
#   #"${@:-${HOME})}" 2>/dev/null)
#   # test $? && echo positive $?
#   # builtin cd "${@:-${HOME}}" || {echo $? && builtin cd "${currentPWD}"}
#   #  && builtin cd "${@:-$(/bin/pwd))}" || echo "ERROR: will stay in $(pwd)"
#   echo ''
#   [[ -x $(which lolcat) ]] && (pwd | lolcat) || ([[ -x $(which /bin/pwd) ]] && /bin/pwd)
#   [[ -x $(which colorls) ]] && (colorls -lA --sd -d --gs) || ([[ -x $(which /bin/ls) ]] && /bin/ls --color=auto -hal)
#   [[ -x $(which lolcat) ]] && (pwd | lolcat) || ([[ -x $(which /bin/pwd) ]] && /bin/pwd)
#   echo ''

# }

# ## Extra code make it easy to copy paste only the function out to use elsewhere
# # cdx_ $@

# unset cdx_

# exit 0
