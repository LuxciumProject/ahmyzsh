#!/usr/bin/env node

// encodeURI

// Get the string from the command line arguments
const [,, ...args] = process.argv;
const stringToEncode = args.join(' ');

// Encode the string using encodeURI
const encodedString = encodeURI(stringToEncode);

// Print the encoded string to the console
console.log(encodedString);
