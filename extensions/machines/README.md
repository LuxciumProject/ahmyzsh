<!-- extensions/machines/README.md -->

# Machine-profile placeholder

Kubuntu is the first supported target. A future machine profile will describe
OS family, package manager, graphical terminal, optional GPU stack, locale,
project root and enabled extension planes as data.

Fedora-specific configuration remains legacy until the portable base is stable.
Host differences must not become new long-lived Git branches.
