<!-- extensions/tmux/README.md -->

# Tmux extension placeholder

The historical simple configuration and custom session framework remain under
`tmux/` and `custom-tmux/`. Neither is loaded by the new backbone.

The future surface will contain one tmux configuration, validated declarative
session specifications, preview-before-apply behavior, and no hard-coded
project paths. Shell startup must not depend on tmux being installed.
