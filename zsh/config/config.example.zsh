# zsh/config/config.example.zsh
# Copy selected overrides to ~/.config/ahmyzsh/config.zsh; do not edit defaults.zsh.

# typeset -g AHMYZSH_PROFILE=1
# typeset -g AHMYZSH_USE_OMZ=0
# typeset -g AHMYZSH_PROMPT=basic
# typeset -g AHMYZSH_KEYMAP=emacs
# typeset -ga AHMYZSH_OMZ_PLUGINS=(git colored-man-pages docker)

# Modules can be removed or reordered explicitly. Preserve dependency order.
# typeset -ga AHMYZSH_MODULES=(
#   base-options core-functions core-commands oh-my-zsh history
#   core-aliases prompt keybindings
# )
